<nav class="sidebar">
    <div style="padding:16px 14px 8px;">
        <div style="font-size:11px;font-weight:500;color:#6B7280;
                    text-transform:uppercase;letter-spacing:.06em;margin-bottom:8px;">
            Principal
        </div>
        <a href="@Url.Action("Index","Encuestas")"
           class="nav-item @(ViewContext.RouteData.Values["controller"].ToString() == "Encuestas" && ViewContext.RouteData.Values["action"].ToString() == "Index" ? "active" : "")">
            <svg width="15" height="15" fill="none" stroke="currentColor" stroke-width="1.8" viewBox="0 0 24 24">
                <rect x="3" y="3" width="7" height="7" rx="1" />
                <rect x="14" y="3" width="7" height="7" rx="1" />
                <rect x="3" y="14" width="7" height="7" rx="1" />
                <rect x="14" y="14" width="7" height="7" rx="1" />
            </svg>
            Mis Encuestas
        </a>
        <a href="@Url.Action("Resultados","Encuestas")"
           class="nav-item @(ViewContext.RouteData.Values["action"].ToString() == "Resultados" ? "active" : "")">
            <svg width="15" height="15" fill="none" stroke="currentColor" stroke-width="1.8" viewBox="0 0 24 24">
                <line x1="18" y1="20" x2="18" y2="10" />
                <line x1="12" y1="20" x2="12" y2="4" />
                <line x1="6" y1="20" x2="6" y2="14" />
            </svg>
            Resultados
        </a>
        <a href="@Url.Action("Exportar","Encuestas")"
           class="nav-item @(ViewContext.RouteData.Values["action"].ToString() == "Exportar" ? "active" : "")">
            <svg width="15" height="15" fill="none" stroke="currentColor" stroke-width="1.8" viewBox="0 0 24 24">
                <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4" />
                <polyline points="7 10 12 15 17 10" />
                <line x1="12" y1="15" x2="12" y2="3" />
            </svg>
            Exportar
        </a>
    </div>

    <div style="padding:8px 14px;border-top:1px solid #E5E4E0;margin-top:8px;">
        <div style="font-size:11px;font-weight:500;color:#6B7280;
                    text-transform:uppercase;letter-spacing:.06em;margin-bottom:8px;">
            Cuenta
        </div>
        <a href="@Url.Action("Perfil","Account")"
           class="nav-item @(ViewContext.RouteData.Values["action"].ToString() == "Perfil" ? "active" : "")">
            <svg width="15" height="15" fill="none" stroke="currentColor" stroke-width="1.8" viewBox="0 0 24 24">
                <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2" />
                <circle cx="12" cy="7" r="4" />
            </svg>
            Perfil
        </a>
        <a href="@Url.Action("Logout","Account")" class="nav-item" style="color:#E03E3E;">
            <svg width="15" height="15" fill="none" stroke="currentColor" stroke-width="1.8" viewBox="0 0 24 24">
                <path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4" />
                <polyline points="16 17 21 12 16 7" />
                <line x1="21" y1="12" x2="9" y2="12" />
            </svg>
            Cerrar sesión
        </a>
    </div>
</nav>